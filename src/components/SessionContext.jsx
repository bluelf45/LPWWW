import { createContext } from 'react';

/* const initialValue = {
	authenticated: false,
	setAuthenticated: () => {},
	tipoUsuario: '',
	setTipoUsuario: () => {},
}; */

export const AuthContext = createContext(null);
